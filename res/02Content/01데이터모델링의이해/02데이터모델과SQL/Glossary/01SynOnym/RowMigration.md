- Row Migration
- Row Migration (로우 마이그레이션)
- RowMigration
- Update
- UPDATE
- 나중에 UPDATE로 데이터가 커져서 원래 블록에 더 이상 저장할 수 없게 될 경우
- 두 개 블록
- 두개 블록
- 두개블록
- 두개블록▶️포인터+데이터
- 두개블록▶️포인터블록+1데이터블록+2데이터블록
- 두개블록▶️포인터블록+데이터블록
- 두개블록▶️포인터블록+데이터블록+데이터블록
- 로우 마이그레이션
- 로우마이그레이션
- 로우의 실제 데이터는 다른 블록으로 이동(migrate) 하지만, 원래 블록에는 포인터만 남음
- 수정
- 수정(UPDATE)
- 수정(UPDATE) 시
- 처음에는 로우가 하나의 블록에 저장됨
- 포인터만 남음
