- Foreign Key
- ForeignKey
- FK
- 외래키
- 테이블생성시 설정가능
- ✅NULL
- 한테이블에 여러개존재가능
- 참조무결성 제약이 기본으로 걸립니다
- 참조무결성 제약이 기본으로 생성
- 참조무결성(Referential Integrity)은 외래키(Foreign Key)가 가리키는 값이 반드시 부모 테이블에 존재해야 한다는 규칙
- 부모 테이블에 존재해야 한다
- 부모 테이블에 존재해야한다
- 부모 테이블에 존재해야함
- 부모테이블에 존재해야함
- ✅외래PK값▶️✅FK값삽입
- ❌외래PK값▶️❌FK값삽입